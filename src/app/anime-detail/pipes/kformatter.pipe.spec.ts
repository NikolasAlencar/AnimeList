import { KformatterPipe } from './kformatter.pipe';

describe('KformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new KformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
